var LiveForm = require("./LiveForm");

module.exports = class Stone extends LiveForm{
    constructor(x, y) {
        super(x, y);
    }

    getNewCoordinates() {
        super.getNewCoordinates();
    }
}